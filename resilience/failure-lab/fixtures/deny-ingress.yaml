apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sample-service-deny-ingress
  namespace: sample-service
  labels:
    failure-lab: network-policy
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: sample-service
  policyTypes:
    - Ingress
