# Developer platform

The interface to the golden path.

* `cli/` - `platform` binary
* `templates/` - later, extra languages
* `golden-paths/` - narrative for humans
* `self-service/` - what is actually requestable
* `service-catalogue/` - YAML ownership, not a portal

```bash
platform version
platform doctor
platform create service payments-api
platform validate
```
