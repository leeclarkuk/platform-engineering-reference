# Self-service

Today: `platform create service`.
Not today: account vending, DNS names, IAM roles beyond the golden
path. Those are still platform-team changes because they are shared
fate. A portal button that creates a VPC peering is how you get an
unmanageable mesh.
