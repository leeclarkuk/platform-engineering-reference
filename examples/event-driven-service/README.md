# Event-driven service

Later increment: a small consumer that shows the same golden path for
workers (probes, metrics, identity) without introducing a mesh or a
second language. HTTP remains the first path because it is easier to
SLO.
