# Runbook: sample-service

1. Check `/ready` and `/metrics`
2. Check Argo CD application
3. Check recent Git deploys
4. If users fail and those are green, you are in DNS, IAM, or
   dependency territory. Use the failure-lab write-ups.
