# Tagging

Enforced keys: `Owner`, `Environment`, `CostCentre`, `Service`,
`DataClassification`, `ManagedBy`.

GCP uses lowercase labels; the meaning is the same.

The AWS tags module is the source of truth for AWS compositions.
Untagged production resources are defects, not a backlog of "tech debt".
