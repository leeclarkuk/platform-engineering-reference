# Azure networking

Hub-and-spoke VNets, gateway and firewall subnets in the hub, spokes
peered with forwarded traffic. VPN on the hub.

Virtual WAN is the scale-up when branches and many regions make
Microsoft-operated hubs the simpler operational model. It is not the
default in this reference. See ADR-005 and `docs/architecture/azure.md`.
