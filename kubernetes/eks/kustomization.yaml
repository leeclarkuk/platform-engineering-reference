apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
# EKS Pod Identity binds IAM to the Kubernetes service account in
# Terraform. No IRSA annotation is required on the golden path.
# irsa-patch.yaml remains as an escape hatch.
